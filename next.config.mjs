/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['www.google.com'], // Add the external domain here
    },
}
export default nextConfig;
